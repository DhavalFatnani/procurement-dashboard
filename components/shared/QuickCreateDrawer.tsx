"use client";

import { FormDrawer } from "@/components/shared/Drawer";

/**
 * @deprecated Use `FormDrawer` from `@/components/shared/Drawer`.
 *
 * Thin compatibility wrapper preserved so existing import paths keep working
 * after the drawer-shell consolidation.
 */
export const QuickCreateDrawer = FormDrawer;
