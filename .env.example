NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Dashboard → Settings → API Keys: publishable key (sb_publishable_...)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
# Server-only — never NEXT_PUBLIC. Replaces legacy service_role JWT.
SUPABASE_SECRET_KEY=sb_secret_...

# Legacy JWT keys (optional migration fallback — prefer publishable + secret above)
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# Database — Prisma 7 connects directly to Supabase via the node-postgres driver
# adapter (see lib/prisma.ts). No Prisma cloud / Accelerate required. Queries run
# in parallel; the pg connection pool (DB_POOL_MAX) bounds real concurrency.
#
# DATABASE_URL = Supabase TRANSACTION pooler (port 6543). Built for serverless:
# it multiplexes many short-lived client connections onto few Postgres ones.
DATABASE_URL="postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@YOUR_POOLER_HOST:6543/postgres?pgbouncer=true"
#
# DIRECT_URL = Supabase SESSION pooler (port 5432). Used ONLY by `prisma migrate`
# (the transaction pooler can't run DDL/migrations).
DIRECT_URL="postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@YOUR_POOLER_HOST:5432/postgres"
#
# Optional: connections per serverless instance (pg pool `max`). Default 5.
# DB_POOL_MAX=5

# Required by `prisma db seed` to provision seed auth users. The seed throws if unset.
SEED_AUTH_PASSWORD=choose-a-strong-password

# Migrations on Supabase: use deploy (no shadow DB). `db:migrate` (migrate dev) fails with P3006
# if the DB was first created with `db push` — only index migrations exist, no baseline.
# npm run db:migrate:deploy

# After linking Supabase, create private storage buckets (fixes "Bucket not found" on upload):
# npm run storage:setup
